spring:
  application:
    name: dubbo-consumer

# Dubbo 配置
dubbo:
  application:
    name: dubbo-consumer
  registry:
    address: nacos://localhost:8848
  consumer:
    timeout: 3000
    retries: 0

# 服务端口
server:
  port: 8082

