<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.lix</groupId>
        <artifactId>consumer</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>consumer-web</artifactId>
    <packaging>jar</packaging>
    <name>consumer-web</name>
    <description>Consumer Web 模块（Controller、DTO、响应封装等）</description>

    <dependencies>
        <!-- Spring Boot Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Cloud Config -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-config</artifactId>
        </dependency>

        <!-- Dubbo Spring Boot Starter -->
        <dependency>
            <groupId>org.apache.dubbo</groupId>
            <artifactId>dubbo-spring-boot-starter</artifactId>
        </dependency>

        <!-- Dubbo 注册中心 - Nacos -->
        <dependency>
            <groupId>org.apache.dubbo</groupId>
            <artifactId>dubbo-registry-nacos</artifactId>
        </dependency>

        <!-- Consumer Facade 模块 -->
        <dependency>
            <groupId>org.lix</groupId>
            <artifactId>consumer-facade</artifactId>
        </dependency>

        <!-- Consumer Common 模块 -->
        <dependency>
            <groupId>org.lix</groupId>
            <artifactId>consumer-common</artifactId>
        </dependency>

        <!-- Consumer DAO 模块 -->
        <dependency>
            <groupId>org.lix</groupId>
            <artifactId>consumer-dao</artifactId>
        </dependency>

        <!-- Consumer Integration 模块 -->
        <dependency>
            <groupId>org.lix</groupId>
            <artifactId>consumer-integration</artifactId>
        </dependency>

        <!-- Consumer Service 模块（业务逻辑） -->
        <dependency>
            <groupId>org.lix</groupId>
            <artifactId>consumer-service</artifactId>
        </dependency>

        <!-- Provider Facade 模块（Dubbo 服务接口，Controller 中需要使用） -->
        <dependency>
            <groupId>org.lix</groupId>
            <artifactId>provider-facade</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>

